QtLogicSimulator (Qt)

LogicSim is a lightweight, graphical digital logic circuit simulator built with C++ and the Qt framework. It allows users to design logic circuits visually using a drag-and-drop interface and simulate their behavior in real-time.
